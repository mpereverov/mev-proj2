---

version: "3.7"

services:

  API:
    image: "{{ img_NAME }}:{{ img_TAG }}"
    container_name: api
    ports: 
      - "4004:4004"

  SPA:
    image: "{{ img_NAME }}:{{ img_TAG }}"
    container_name: spa
    ports: 
      - "4003:4003"
   